{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/todolist-frontend/package.json",
                "localRoot": "${workspaceFolder}/todolist-frontend"
            }
        }
    ]
}